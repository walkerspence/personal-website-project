/*
	to run tests, use the terminal command "npm test test/numberguess.spec". This will run tests for all parts of the number guess assignment.
*/

function higher_or_lower(guess, number_to_guess) {
	/* inputs: guess: the player's guess
		  	   number_to_guess: the number the player is trying to guess.

   	   returns: "higher" if number_to_guess is higher than guess, "winner" if number_to_guess is exactly guess,  and "lower" otherwise
	*/
}

module.exports = { higher_or_lower: higher_or_lower }

